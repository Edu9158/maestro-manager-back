version: '3.8'

services:
  maestro-app:
    build: .
    container_name: maestro_back
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
      - PORT=8000
    restart: always